Font upload for personal use in Vencord via Custom CSS.</br>
</br>


<sub><ins>**Note**</ins>: When using with languages other than English, must change `:root` to `:root:lang(language)` where *language* is the language you have Discord set to.</sub></br>
<sub>Ex. `:root:lang(zh-CN)`</sub>
